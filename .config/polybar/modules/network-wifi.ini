[module/network-wifi]
type = internal/network
interface = wlp1s0
interval = 3.0

label-connected-font = 2
label-disconnected-font = 2

format-connected = <label-connected>
format-connected-background = ${root.network-wifi-background}
format-connected-foreground = ${root.network-wifi-foreground}
format-connected-padding = 1
format-connected-prefix = "直 "

label-connected = %essid%

format-disconnected = <label-disconnected>
format-disconnected-background = ${root.network-wifi-background}
format-disconnected-foreground = ${root.network-wifi-foreground}
format-disconnected-padding = 1

format-disconnected-prefix = "睊 "

label-disconnected = Offline

