[module/window-title]
type = internal/xwindow

format = <label>
format-foreground = ${root.window-title-foreground}
format-background = ${root.window-title-background}

label = %title%
label-padding = 1
label-empty =
label-maxlen = 29
